# Environment Variables Template

# Copy this file to .env.local and fill in your credentials.
# NEVER commit .env.local or any file containing real secrets.

#   cp .env.example .env.local

# Node Environment
NODE_ENV=development 

# MongoDB Atlas
# Full Atlas connection string including username, password, and cluster host.
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/

# Database name within the Atlas cluster
MONGODB_DB=torbidd

# Google Cloud / Vertex AI  (server-side only — never expose to the browser)
# Your GCP project ID
GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Region where Vertex AI endpoints are deployed
GOOGLE_CLOUD_LOCATION=asia-southeast1

# Gemini model for full TOR extraction (multimodal PDF understanding)
VERTEX_AI_MODEL_PRO=gemini-1.5-pro

# Gemini model for fast project classification
VERTEX_AI_MODEL_FLASH=gemini-1.5-flash

# Absolute path to the GCP service account JSON key file.
# In Docker / CI: mount this file as a secret or base64-encode via GCP_SA_KEY.
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Application
# Public base URL — used for OpenGraph, canonical URLs, and API redirects
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Secret used to sign session cookies (generate with: openssl rand -hex 32)
SESSION_SECRET=change-me-to-a-long-random-string
